# Extension Store (#138) — progress ledger
Plan: ~/Documents/vault/sorayomi/plans/2026-07-23-extension-store-plan.md
Branch: extension-store (base f2a88264, worktree .claude/worktrees/extension-store)
Task 1: complete (commit 891d9ae1, review clean). Minor (defer to final): refresh-failure-after-successful-mutation toast edge case (screen add/delete flows); doc-shape-only test coverage ceiling.
Task 2: complete (commits 8f08a570 + e207dfe5, review approved). Schema = v2.3.2243, all 8 hard-gate items pass; forced fixes: AllCategories order args, bindTrackRecord nullability. Legacy-field sites carry intent-marker ignores (analyzer 12.1.0 doesn't flag same-package deprecation; markers are future-proofing). FYI for later tasks: fetchManga/fetchChapters + versionCode now deprecated; ExtensionFilterInput swapped isNsfw/repo→contentWarning.
Task 3: complete (commits 9fd6c6bb + 110ba5cd, 8 tests pass, analyze clean, full suite 1178/1178). ExtensionStoreRepository probe + classifyStoreProbe classifier + extensionStoreRepositoryProvider/extensionStoreSupportProvider; CRUD deferred to Task 4. Review-driven additions: cache-contract tests via scripted Link (transient-not-memoized, definitive-memoized) + classifier edge cases. Note for later tasks: tracker_repository's Expando cache memoizes transient failures permanently (??= pattern) — the store probe's clear-on-null is stricter; consider backporting.
Task 3: complete (commits 9fd6c6bb + e1d3f0c7, review approved). Probe/classifier verified against graphql-5.2.4 source. Minor (defer to final): broad catch-all in _probe classifies programming errors as transient (matches sibling pattern); no test for caller-during-in-flight-probe. BACKPORT CANDIDATE (post-branch): tracker_repository.dart:138-139 ??= permanently memoizes transient capability failures — same bug class this task avoided.
Task 4: complete (commits 002df635 + 7f3c87f6, review approved, zero findings). Store fragment/documents/CRUD/list provider in. CRUD tests intentionally deferred to Task 6 widget tests + Task 8 live verify.
Task 5: complete (commit 5f1b861e, review approved, 3 mapper tests pass, analyze clean). getExtensionListStream gates on ExtensionStoreRepository.supportsExtensionStores(); store-capable path uses contentWarning -> isNsfw allowlist mapper (extensionFromStoreDto), legacy path untouched. No repository-level branching test yet (mapper is pure-function tested; branching itself deferred, consistent with the Task 4 CRUD-test deferral pattern) — flag before final if that gap should close.
Task 5: complete (commits 5f1b861e + 33949b21, review approved). Allowlist mapper verified over generated enum ($unknown⇒18+); legacy path byte-identical; Expando shared, no double probe. Minor (defer to final/T8): no direct test of the store-vs-legacy branch in getExtensionListStream (covered by T8 live verify).
Task 6: complete (commits c3db4d72 + 96c71d0f, review approved). Locked strings byte-verified; all 5 deviations hold; route registered but unwired (by design). Minor (defer to final): two `// ignore: unused_result` suppressions on fire-and-forget refreshes; report undersold that `add` arb key is new.
Task 7: complete (review approved, 9 gate tests pass, analyze clean, 77/77 across browse_center+settings). browse_screen.dart tab-1 icon + browse_settings_screen.dart repo row now branch on `extensionStoreSupportProvider.value ?? false` (riverpod 3.3.2 has no separate `valueOrNull` — `.value` is already the safe nullable accessor). Review flagged missing error-state coverage; closed with two added tests (icon + row) overriding the provider with a throwing future. No open findings.
